# Optional: Set a default OpenRouter API key for the hosted instance.
# Users can also enter their own key in the Settings UI.
OPENROUTER_API_KEY=

# Optional: Set a default Google API key for Gemini TTS.
# Users can also enter their own key in the Settings UI.
# Get a free key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=
